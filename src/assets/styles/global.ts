import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
    screenContainer: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    },
    textField: {
        borderRadius: 10,
        borderColor: "#000",
        borderWidth: 1,
        padding: 12
    }
});